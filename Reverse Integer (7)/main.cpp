#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <map>
#include <utility>

using namespace std;


class Solution {
public:
	int reverse(int x) {
		int ans;
		bool positive;
		if (abs(x) / 10 == 0) return x;
		positive = (x < 0) ? false : true;
		for (int i = 0; i < )
	}


	int num_digits(int x) {
		int ans = 0;
		while (x != 0)
	}
};

int main() {

}